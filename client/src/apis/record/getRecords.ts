export interface RecordType {
    category: string;
    startDate: string;
    endDate: string;
}

const getRecords = (): RecordType[] => {
    return [];
};

const useGetRecords = () => {};

export default useGetRecords;
